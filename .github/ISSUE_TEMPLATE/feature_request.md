---
name: Feature request
about: Propose a capability for Loom
title: "[feat] "
labels: enhancement
---

**The problem you're solving** (not the solution — the problem):

**Why it can't be composed from the five existing primitives**
(`State`/`Step`/`Router`/`Graph`/`Store`). Loom is deliberately minimal;
proposals that can be expressed as stdlib compositions of existing primitives
will be pointed that way — please show you checked.

**Proposed shape** (API sketch, which layer it lives in):

**Prior art** (optional): how LangGraph / other frameworks handle this.
