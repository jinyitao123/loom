// Package tests contains the black-box test suite for the loom module.
// These tests exercise the public API only (kernel, contract, stdlib);
// white-box tests that need kernel internals stay next to the kernel
// files at the repository root (e.g. graph_budget_resume_test.go).
package tests
